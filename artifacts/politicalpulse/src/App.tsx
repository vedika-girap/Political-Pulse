import { useEffect, useMemo, useState } from 'react';
import { Link, Route, Switch, useLocation } from 'wouter';
import { BarChart3, BookOpen, ChevronDown, CircleHelp, Database, FileText, Filter, Globe2, Hash, LayoutDashboard, Menu, MessageCircle, Network, PanelLeftClose, Search, Sparkles, Table2, X, Users, Activity, ExternalLink } from 'lucide-react';
import { Area, AreaChart, Bar, BarChart, Cell, Line, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { DEMO_COMMENTS, DEMO_POSTS, DEMO_TOPICS, getEngagement, getInsights, getLanguages, getOverview, getPeers, getPlatforms, getSentiment } from '@/services/api';
import type { Comment, Insight, Post } from '@/types';
import './index.css';

const navGroups = [
  { label: 'Observe', items: [{ href: '/dashboard', label: 'Overview', icon: LayoutDashboard }, { href: '/posts', label: 'Post explorer', icon: FileText }, { href: '/engagement', label: 'Engagement', icon: Activity }, { href: '/audience-response', label: 'Audience response', icon: MessageCircle }] },
  { label: 'Understand', items: [{ href: '/topics', label: 'Topics', icon: Hash }, { href: '/language', label: 'Language', icon: Globe2 }, { href: '/platforms', label: 'Platforms', icon: Network }, { href: '/peer-benchmarking', label: 'Peer benchmarking', icon: Users }, { href: '/ai-insights', label: 'Analyst notes', icon: Sparkles }] },
  { label: 'Provenance', items: [{ href: '/data-sources', label: 'Data sources', icon: Database }, { href: '/data-import', label: 'Data import', icon: Table2 }, { href: '/methodology', label: 'Methodology', icon: BookOpen }] },
];

function DemoBanner() { return <div className="flex items-center justify-center gap-2 bg-[hsl(var(--accent))] px-4 py-2 text-[11px] font-semibold uppercase tracking-[.16em] text-[hsl(var(--accent-foreground))]" data-testid="banner-demo"><span className="h-1.5 w-1.5 rounded-full bg-[hsl(var(--primary))]" />DEMO DATA — Replace with real dataset</div>; }

function Sidebar({ open, onClose }: { open: boolean; onClose: () => void }) {
  const [location] = useLocation();
  return <aside className={`fixed inset-y-0 left-0 z-40 flex w-[272px] flex-col bg-[hsl(var(--sidebar))] text-[hsl(var(--sidebar-foreground))] transition-transform duration-300 md:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}>
    <div className="flex h-[84px] items-center justify-between border-b border-[hsl(var(--sidebar-border))] px-6"><Link href="/dashboard" className="flex items-center gap-3" data-testid="link-brand"><span className="grid h-9 w-9 place-items-center rounded-[11px] bg-[hsl(var(--accent))] text-[hsl(var(--sidebar))]"><Activity size={18} strokeWidth={2.6} /></span><span><b className="block font-serif text-[23px] leading-none text-[hsl(var(--sidebar-accent-foreground))]">PoliticalPulse</b><small className="font-mono text-[9px] uppercase tracking-[.22em] text-[hsl(var(--sidebar-foreground)/.68)]">Research workspace</small></span></Link><button className="md:hidden" onClick={onClose} aria-label="Close navigation" data-testid="button-close-navigation"><X size={18}/></button></div>
    <div className="border-b border-[hsl(var(--sidebar-border))] px-6 py-5"><p className="mb-1 font-mono text-[9px] uppercase tracking-[.16em] text-[hsl(var(--sidebar-foreground)/.58)]">Subject under study</p><p className="text-sm font-semibold text-[hsl(var(--sidebar-accent-foreground))]">Darshan Puttannaiah, MLA</p><p className="mt-1 text-xs text-[hsl(var(--sidebar-foreground)/.7)]">Melukote · Karnataka</p><div className="mt-3 inline-flex rounded-full border border-[hsl(var(--sidebar-border))] px-2 py-1 text-[10px] text-[hsl(var(--sidebar-foreground)/.76)]">Sarvodaya Karnataka Paksha</div></div>
    <nav className="flex-1 overflow-y-auto px-4 py-5">{navGroups.map(group => <div className="mb-6" key={group.label}><p className="mb-2 px-3 font-mono text-[9px] uppercase tracking-[.18em] text-[hsl(var(--sidebar-foreground)/.48)]">{group.label}</p>{group.items.map(item => { const Icon = item.icon; const active = location === item.href; return <Link key={item.href} href={item.href} onClick={onClose} className={`group mb-1 flex items-center gap-3 rounded-lg px-3 py-2.5 text-[13px] transition-colors ${active ? 'bg-[hsl(var(--sidebar-accent))] font-semibold text-[hsl(var(--sidebar-accent-foreground))]' : 'text-[hsl(var(--sidebar-foreground)/.76)] hover:bg-[hsl(var(--sidebar-accent)/.65)] hover:text-[hsl(var(--sidebar-accent-foreground))]'}`} data-testid={`link-nav-${item.href.slice(1)}`}><Icon size={16} strokeWidth={active ? 2.5 : 1.8} /><span>{item.label}</span>{active && <span className="ml-auto h-1.5 w-1.5 rounded-full bg-[hsl(var(--accent))]" />}</Link> })}</div>)}</nav>
    <div className="border-t border-[hsl(var(--sidebar-border))] px-6 py-4"><div className="flex items-center justify-between"><span className="font-mono text-[9px] uppercase tracking-[.16em] text-[hsl(var(--sidebar-foreground)/.48)]">Collection window</span><span className="font-mono text-[10px] text-[hsl(var(--sidebar-foreground)/.76)]">12 weeks</span></div><div className="mt-3 h-1 rounded-full bg-[hsl(var(--sidebar-border))]"><div className="h-full w-[92%] rounded-full bg-[hsl(var(--accent))]" /></div></div>
  </aside>;
}

function Shell({ children }: { children: React.ReactNode }) { const [mobileOpen, setMobileOpen] = useState(false); return <div className="noise min-h-[100dvh] bg-[hsl(var(--background))]"><DemoBanner/><Sidebar open={mobileOpen} onClose={() => setMobileOpen(false)}/>{mobileOpen && <button className="fixed inset-0 z-30 bg-[hsl(var(--foreground)/.35)] md:hidden" onClick={() => setMobileOpen(false)} aria-label="Close navigation overlay" data-testid="button-overlay" /> }<main className="min-h-[calc(100dvh-32px)] md:pl-[272px]"><header className="sticky top-0 z-20 flex h-[72px] items-center justify-between border-b border-[hsl(var(--border)/.8)] bg-[hsl(var(--background)/.9)] px-5 backdrop-blur-md md:px-10"><div className="flex items-center gap-3"><button className="rounded-lg p-2 hover:bg-[hsl(var(--muted))] md:hidden" onClick={() => setMobileOpen(true)} aria-label="Open navigation" data-testid="button-open-navigation"><Menu size={20}/></button><div className="hidden items-center gap-2 text-xs text-[hsl(var(--muted-foreground))] md:flex"><span>Workspace</span><span>/</span><span className="text-[hsl(var(--foreground))]">Public activity study</span></div></div><div className="flex items-center gap-3"><button className="hidden items-center gap-2 rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 py-2 text-xs text-[hsl(var(--muted-foreground))] sm:flex" data-testid="button-date-window"><span className="h-1.5 w-1.5 rounded-full bg-[hsl(var(--chart-3))]"/> Jun 01 — Sep 02, 2024 <ChevronDown size={14}/></button><button className="grid h-9 w-9 place-items-center rounded-full border border-[hsl(var(--border))] bg-[hsl(var(--card))] text-xs font-bold text-[hsl(var(--primary))]" data-testid="button-user-menu">PP</button></div></header>{children}</main></div>; }

function SectionIntro({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: React.ReactNode }) { return <div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-2 font-mono text-[10px] uppercase tracking-[.18em] text-[hsl(var(--primary))]">{eyebrow}</p><h1 className="font-serif text-4xl tracking-[-.02em] text-[hsl(var(--foreground))] md:text-[46px]">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[hsl(var(--muted-foreground))]">{description}</p></div>{action}</div>; }
function Card({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-xl border border-[hsl(var(--card-border))] bg-[hsl(var(--card))] shadow-[0_1px_0_hsl(var(--foreground)/.02)] ${className}`}>{children}</section>; }
function CardHead({ title, meta }: { title: string; meta?: string }) { return <div className="flex items-center justify-between border-b border-[hsl(var(--border)/.7)] px-5 py-4"><h2 className="text-[13px] font-semibold">{title}</h2>{meta && <span className="font-mono text-[10px] text-[hsl(var(--muted-foreground))]">{meta}</span>}</div>; }
function Metric({ label, value, note, accent = false }: { label: string; value: string; note: string; accent?: boolean }) { return <Card className={`fade-up p-5 ${accent ? 'border-[hsl(var(--accent)/.65)] bg-[hsl(var(--accent)/.13)]' : ''}`}><p className="font-mono text-[10px] uppercase tracking-[.12em] text-[hsl(var(--muted-foreground))]">{label}</p><p className="mt-3 text-3xl font-semibold tracking-[-.04em]">{value}</p><p className="mt-2 text-xs text-[hsl(var(--muted-foreground))]">{note}</p></Card>; }
function LoadingCards() { return <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{Array.from({length: 6}, (_, i) => <div key={i} className="h-[130px] animate-pulse rounded-xl bg-[hsl(var(--muted))]" />)}</div>; }
function EmptyState({ title, text }: { title: string; text: string }) { return <div className="grid min-h-[220px] place-items-center p-8 text-center"><div><div className="mx-auto mb-3 grid h-10 w-10 place-items-center rounded-full bg-[hsl(var(--muted))] text-[hsl(var(--primary))]"><Search size={17}/></div><h3 className="text-sm font-semibold">{title}</h3><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{text}</p></div></div>; }

function FilterBar({ filters, setFilters }: { filters: Record<string,string>; setFilters: (x: Record<string,string>) => void }) { const set = (key: string, value: string) => setFilters({ ...filters, [key]: value }); const fields: Array<[string, string, string[]]> = [['platform','Platform',['All platforms','Facebook','Instagram','YouTube','X']],['topic','Topic',['All topics','Local governance','Agriculture','Education','Environment','Public events']],['language','Language',['All languages','Kannada','English','Bilingual']],['sentiment','Sentiment',['All sentiments','Positive','Neutral','Critical']],['mediaType','Media',['All media','Video','Image','Text','Link']]]; return <div className="mb-7 flex flex-wrap items-center gap-2 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card)/.65)] p-3"><Filter size={15} className="mx-1 text-[hsl(var(--muted-foreground))]" />{fields.map(([key, label, options]) => <label key={key} className="relative"><span className="sr-only">{label}</span><select value={filters[key] || options[0]} onChange={e => set(key, e.target.value)} className="h-9 appearance-none rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--background))] py-1 pl-3 pr-8 text-xs outline-none focus:border-[hsl(var(--primary))]" data-testid={`select-filter-${key}`}>{options.map((option: string) => <option key={option}>{option}</option>)}</select><ChevronDown className="pointer-events-none absolute right-2 top-3 text-[hsl(var(--muted-foreground))]" size={12}/></label>)}<button onClick={() => setFilters({})} className="ml-auto flex h-9 items-center gap-2 rounded-lg px-3 text-xs text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]" data-testid="button-clear-filters"><X size={13}/> Clear</button></div>; }

function Overview() { const [data, setData] = useState<any>(); const [engagement, setEngagement] = useState<any[]>([]); const [sentiment, setSentiment] = useState<any[]>([]); const [languages, setLanguages] = useState<any[]>([]); const [filters, setFilters] = useState<Record<string,string>>({}); const [selectedPost, setSelectedPost] = useState<Post | null>(null); useEffect(() => { Promise.all([getOverview(), getEngagement(), getSentiment(), getLanguages()]).then(([a,b,c,d]) => { setData(a);setEngagement(b);setSentiment(c);setLanguages(d); }); }, []); const filtered = useMemo(() => DEMO_POSTS.filter(post => Object.entries(filters).every(([key, value]) => !value || value.startsWith('All') || post[key as keyof Post] === value)), [filters]); if (!data) return <Page><SectionIntro eyebrow="Observatory / Overview" title="Reading the pulse" description="A measured view of public social activity around Melukote."/><LoadingCards/></Page>; return <Page><SectionIntro eyebrow="Observatory / Overview" title="Reading the pulse" description="A measured view of public social activity around Melukote. Start with the shape of the dataset, then follow the signals into their context." action={<button className="flex items-center gap-2 rounded-lg bg-[hsl(var(--primary))] px-4 py-2.5 text-xs font-semibold text-[hsl(var(--primary-foreground))] transition-transform hover:-translate-y-0.5" data-testid="button-export-overview"><ExternalLink size={14}/> Export view</button>}/><FilterBar filters={filters} setFilters={setFilters}/><div className="mb-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6"><Metric label="Total posts" value={String(data.totalPosts)} note="Across 4 channels"/><Metric label="Total comments" value={String(data.totalComments)} note="Observed responses"/><Metric label="Total engagement" value={data.totalEngagement.toLocaleString()} note="Likes, replies, shares" accent/><Metric label="Average engagement" value={data.averageEngagement.toLocaleString()} note="Per published post"/><Metric label="Active platforms" value={String(data.activePlatforms)} note="Connected channels"/><Metric label="Data coverage" value={`${data.dataCoverage}%`} note="Jun 01 — Sep 02, 2024"/></div><div className="grid gap-4 lg:grid-cols-[1.6fr_1fr]"><Card><CardHead title="Engagement over time" meta="12 week observation"/><div className="h-[290px] p-4"><ResponsiveContainer width="100%" height="100%"><AreaChart data={engagement}><defs><linearGradient id="eng" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#2f6478" stopOpacity={.26}/><stop offset="95%" stopColor="#2f6478" stopOpacity={0}/></linearGradient></defs><XAxis dataKey="date" tick={{fontSize:10}} tickFormatter={v => v.slice(5)} axisLine={false} tickLine={false}/><YAxis tick={{fontSize:10}} axisLine={false} tickLine={false}/><Tooltip contentStyle={{borderRadius:8,border:'1px solid #d8e0e5',fontSize:12}}/><Area type="monotone" dataKey="engagement" stroke="#2f6478" strokeWidth={2.5} fill="url(#eng)" /></AreaChart></ResponsiveContainer></div></Card><Card><CardHead title="Sentiment distribution" meta="classified posts"/><div className="flex h-[290px] items-center justify-center gap-4 p-5"><ResponsiveContainer width="55%" height="80%"><PieChart><Pie data={sentiment} dataKey="value" nameKey="name" innerRadius={50} outerRadius={82} paddingAngle={3}>{sentiment.map(item => <Cell key={item.name} fill={item.color}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><div className="space-y-3">{sentiment.map(item => <div key={item.name} className="flex items-center gap-2 text-xs"><span className="h-2.5 w-2.5 rounded-full" style={{background:item.color}}/><span>{item.name}</span><b className="font-mono text-[10px]">{item.value}%</b></div>)}</div></div></Card></div><div className="mt-4 grid gap-4 lg:grid-cols-3"><Card><CardHead title="Posts by platform"/><div className="h-[240px] p-4"><ResponsiveContainer width="100%" height="100%"><BarChart data={['Facebook','Instagram','YouTube','X'].map(name => ({name, value: filtered.filter(p=>p.platform===name).length}))}><XAxis dataKey="name" tick={{fontSize:10}} axisLine={false} tickLine={false}/><YAxis hide/><Tooltip/><Bar dataKey="value" fill="#2f6478" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div></Card><Card><CardHead title="Topic distribution"/><div className="space-y-4 p-5">{DEMO_TOPICS.map(topic => <div key={topic.name}><div className="mb-1 flex justify-between text-xs"><span>{topic.name}</span><span className="font-mono text-[10px]">{topic.value}%</span></div><div className="h-2 rounded-full bg-[hsl(var(--muted))]"><div className="h-2 rounded-full" style={{width:`${topic.value * 2.7}%`,background:topic.color}}/></div></div>)}</div></Card><Card><CardHead title="Language distribution"/><div className="space-y-5 p-5">{languages.map(item => <div key={item.name} className="flex items-center justify-between border-b border-[hsl(var(--border)/.7)] pb-3 text-xs last:border-0"><span>{item.name}</span><span className="font-mono text-[hsl(var(--primary))]">{item.value}%</span></div>)}</div></Card></div><Card className="mt-4"><CardHead title="Top performing posts" meta={`${filtered.length} posts in view`}/><PostTable posts={filtered.slice().sort((a,b)=>b.engagement-a.engagement).slice(0,5)} onSelect={setSelectedPost}/></Card>{selectedPost && <PostDrawer post={selectedPost} onClose={() => setSelectedPost(null)}/>}</Page>; }

function PostTable({ posts, onSelect }: { posts: Post[]; onSelect: (post: Post) => void }) { if (!posts.length) return <EmptyState title="No posts match these filters" text="Clear one or more filters to widen the observation."/>; return <div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead><tr className="border-b border-[hsl(var(--border))] text-[10px] uppercase tracking-[.1em] text-[hsl(var(--muted-foreground))]"><th className="px-5 py-3 font-mono font-normal">Post</th><th className="px-4 py-3 font-mono font-normal">Platform</th><th className="px-4 py-3 font-mono font-normal">Topic</th><th className="px-4 py-3 font-mono font-normal">Published</th><th className="px-5 py-3 text-right font-mono font-normal">Engagement</th></tr></thead><tbody>{posts.map(post => <tr key={post.id} onClick={() => onSelect(post)} className="cursor-pointer border-b border-[hsl(var(--border)/.65)] transition-colors last:border-0 hover:bg-[hsl(var(--muted)/.55)]" data-testid={`row-post-${post.id}`}><td className="max-w-[310px] px-5 py-4"><p className="truncate font-semibold">{post.title}</p><p className="mt-1 truncate text-[11px] text-[hsl(var(--muted-foreground))]">{post.excerpt}</p></td><td className="px-4 py-4"><span className="rounded-full bg-[hsl(var(--muted))] px-2 py-1 text-[10px]">{post.platform}</span></td><td className="px-4 py-4 text-[hsl(var(--muted-foreground))]">{post.topic}</td><td className="whitespace-nowrap px-4 py-4 font-mono text-[10px] text-[hsl(var(--muted-foreground))]">{post.date}</td><td className="px-5 py-4 text-right font-mono text-[11px] font-medium text-[hsl(var(--primary))]">{post.engagement.toLocaleString()}</td></tr>)}</tbody></table></div>; }
function PostDrawer({ post, onClose }: { post: Post; onClose: () => void }) { return <div className="fixed inset-0 z-50 flex justify-end bg-[hsl(var(--foreground)/.35)]" onClick={onClose}><div onClick={e=>e.stopPropagation()} className="h-full w-full max-w-[480px] overflow-y-auto bg-[hsl(var(--card))] p-6 shadow-2xl animate-[slideIn_.3s_ease-out]" data-testid="drawer-post-detail"><div className="mb-10 flex items-center justify-between"><span className="font-mono text-[10px] uppercase tracking-[.14em] text-[hsl(var(--primary))]">Post detail / {post.id}</span><button onClick={onClose} aria-label="Close post detail" data-testid="button-close-post-drawer"><X size={18}/></button></div><span className="rounded-full bg-[hsl(var(--muted))] px-2.5 py-1 text-[10px]">{post.platform}</span><h2 className="mt-5 font-serif text-4xl leading-tight">{post.title}</h2><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">{post.excerpt}</p><div className="my-8 grid grid-cols-3 gap-2">{[['Likes',post.likes],['Comments',post.comments],['Shares',post.shares]].map(([label,value])=><div key={label as string} className="rounded-lg bg-[hsl(var(--muted)/.7)] p-3"><p className="font-mono text-[9px] uppercase text-[hsl(var(--muted-foreground))]">{label}</p><p className="mt-2 text-lg font-semibold">{value}</p></div>)}</div><dl className="space-y-4 border-t border-[hsl(var(--border))] pt-5 text-xs"><div className="flex justify-between"><dt className="text-[hsl(var(--muted-foreground))]">Published</dt><dd className="font-mono">{post.date}</dd></div><div className="flex justify-between"><dt className="text-[hsl(var(--muted-foreground))]">Topic</dt><dd>{post.topic}</dd></div><div className="flex justify-between"><dt className="text-[hsl(var(--muted-foreground))]">Language</dt><dd>{post.language}</dd></div><div className="flex justify-between"><dt className="text-[hsl(var(--muted-foreground))]">Media type</dt><dd>{post.mediaType}</dd></div><div className="flex justify-between"><dt className="text-[hsl(var(--muted-foreground))]">Sentiment</dt><dd>{post.sentiment}</dd></div></dl></div></div>; }

function Posts() { const [filters, setFilters] = useState<Record<string,string>>({}); const [selected, setSelected] = useState<Post | null>(null); const [query, setQuery] = useState(''); const posts = DEMO_POSTS.filter(post => Object.entries(filters).every(([key, value]) => !value || value.startsWith('All') || post[key as keyof Post] === value)).filter(post => `${post.title} ${post.excerpt}`.toLowerCase().includes(query.toLowerCase())); return <Page><SectionIntro eyebrow="Observe / Post explorer" title="Post explorer" description="Inspect the full observation set with filters that keep the evidence trail visible." action={<div className="flex items-center gap-2 rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3"><Search size={15} className="text-[hsl(var(--muted-foreground))]"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search posts" className="h-10 w-36 bg-transparent text-xs outline-none" data-testid="input-search-posts"/></div>}/><FilterBar filters={filters} setFilters={setFilters}/><Card><CardHead title="Observed posts" meta={`${posts.length} results`}/><PostTable posts={posts} onSelect={setSelected}/></Card>{selected && <PostDrawer post={selected} onClose={()=>setSelected(null)}/>}</Page>; }

function Comments() { const [selected, setSelected] = useState<Comment | null>(null); return <Page><SectionIntro eyebrow="Observe / Audience response" title="Audience response" description="Read the public replies attached to the observed posts. This is context, not a measure of voter preference."/><div className="grid gap-4 lg:grid-cols-[1.5fr_1fr]"><Card><CardHead title="Comment explorer" meta={`${DEMO_COMMENTS.length} responses`}/><div className="divide-y divide-[hsl(var(--border)/.7)]">{DEMO_COMMENTS.slice(0,14).map(comment=><button onClick={()=>setSelected(comment)} key={comment.id} className="block w-full p-5 text-left transition-colors hover:bg-[hsl(var(--muted)/.55)]" data-testid={`button-comment-${comment.id}`}><div className="flex items-center justify-between"><span className="text-xs font-semibold">{comment.author}</span><span className="font-mono text-[10px] text-[hsl(var(--muted-foreground))]">{comment.date}</span></div><p className="mt-2 text-xs leading-5 text-[hsl(var(--muted-foreground))]">{comment.text}</p><div className="mt-3 flex gap-2"><span className="rounded-full bg-[hsl(var(--muted))] px-2 py-1 text-[10px]">{comment.platform}</span><span className="rounded-full bg-[hsl(var(--muted))] px-2 py-1 text-[10px]">{comment.sentiment}</span></div></button>)}</div></Card><Card><CardHead title="Response mix" meta="all comments"/><div className="space-y-5 p-5">{['Neutral','Positive','Critical'].map((label,i)=><div key={label}><div className="mb-1 flex justify-between text-xs"><span>{label}</span><span className="font-mono text-[10px]">{[41,38,21][i]}%</span></div><div className="h-2 rounded-full bg-[hsl(var(--muted))]"><div className="h-full rounded-full" style={{width:`${[41,38,21][i]}%`,background:['#8c9aa3','#5c8e7b','#9a6f63'][i]}}/></div></div>)}<div className="mt-8 rounded-lg border border-dashed border-[hsl(var(--border))] p-4 text-xs leading-5 text-[hsl(var(--muted-foreground))]">Sentiment labels are synthetic classifications for interface demonstration, not conclusions about public opinion.</div></div></Card></div>{selected && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[hsl(var(--foreground)/.35)] p-4" onClick={()=>setSelected(null)}><div className="w-full max-w-md rounded-xl bg-[hsl(var(--card))] p-6 shadow-2xl" onClick={e=>e.stopPropagation()} data-testid="modal-comment-detail"><div className="flex justify-between"><span className="font-mono text-[10px] uppercase tracking-wider text-[hsl(var(--primary))]">Comment detail</span><button onClick={()=>setSelected(null)} data-testid="button-close-comment-modal"><X size={18}/></button></div><h3 className="mt-6 text-lg font-semibold">{selected.author}</h3><p className="mt-3 text-sm leading-6">{selected.text}</p><p className="mt-6 text-xs text-[hsl(var(--muted-foreground))]">{selected.platform} · {selected.date} · {selected.likes} likes</p></div></div>}</Page>; }

function AnalyticsPage({ type }: { type: string }) { const [rows, setRows] = useState<any[]>([]); useEffect(()=>{ const load = type === 'engagement' ? getEngagement : type === 'topics' ? getSentiment : type === 'language' ? getLanguages : type === 'platforms' ? getPlatforms : type === 'peer-benchmarking' ? getPeers : getInsights; load().then(setRows); }, [type]); const titles: Record<string,[string,string,string]> = { engagement:['Engagement','The shape of attention','Track how interactions accumulate across the observation window.'], topics:['Topics','What the conversation is about','A directional view of recurring subjects in the sample.'], language:['Language','Across scripts and registers','How public activity is distributed across language labels.'], platforms:['Platforms','Where activity is visible','The observed channel mix, kept distinct from audience size.'], 'peer-benchmarking':['Peer benchmarking','Context without competition','A synthetic reference set for reading scale, not ranking people.'], 'ai-insights':['Analyst notes','Signals with caveats','Machine-assisted labels are presented as prompts for human review.'] }; const [eyebrow,title,description] = titles[type]; return <Page><SectionIntro eyebrow={`Understand / ${eyebrow}`} title={title} description={description}/>{type === 'engagement' && <Card><CardHead title="Engagement over time" meta="synthetic series"/><div className="h-[380px] p-6"><ResponsiveContainer width="100%" height="100%"><AreaChart data={rows}><XAxis dataKey="date" tickFormatter={v=>v.slice(5)} tick={{fontSize:10}} axisLine={false} tickLine={false}/><YAxis tick={{fontSize:10}} axisLine={false} tickLine={false}/><Tooltip/><Area dataKey="engagement" stroke="#2f6478" fill="#2f6478" fillOpacity={.15} strokeWidth={2.5}/><Line dataKey="posts" stroke="#d59d3f" strokeWidth={2}/></AreaChart></ResponsiveContainer></div></Card>}{type === 'topics' && <div className="grid gap-4 md:grid-cols-2">{DEMO_TOPICS.map(topic=><Card key={topic.name} className="p-6"><div className="flex justify-between text-sm"><h3 className="font-semibold">{topic.name}</h3><span className="font-mono text-[hsl(var(--primary))]">{topic.value}%</span></div><div className="mt-6 h-3 rounded-full bg-[hsl(var(--muted))]"><div className="h-full rounded-full" style={{width:`${topic.value*2.7}%`,background:topic.color}}/></div><p className="mt-4 text-xs leading-5 text-[hsl(var(--muted-foreground))]">Recurring references across the observed post set.</p></Card>)}</div>}{type === 'language' && <Card><CardHead title="Language distribution"/><div className="grid gap-4 p-6 sm:grid-cols-3">{rows.map((row:any)=><div key={row.name} className="rounded-xl bg-[hsl(var(--muted)/.65)] p-5"><p className="text-xs text-[hsl(var(--muted-foreground))]">{row.name}</p><p className="mt-3 text-3xl font-semibold">{row.value}%</p><div className="mt-5 h-1.5 rounded-full bg-[hsl(var(--border))]"><div className="h-full rounded-full bg-[hsl(var(--primary))]" style={{width:`${row.value}%`}}/></div></div>)}</div></Card>}{type === 'platforms' && <div className="grid gap-4 md:grid-cols-2">{rows.map((row:any)=><Card key={row.id} className="p-6"><div className="flex items-center justify-between"><h3 className="font-semibold">{row.platform}</h3><span className="text-[10px] text-[hsl(var(--chart-3))]">{row.verified ? 'Verified account' : 'Observed account'}</span></div><p className="mt-3 text-sm text-[hsl(var(--muted-foreground))]">{row.handle}</p><p className="mt-7 font-mono text-2xl">{row.followers.toLocaleString()}</p><p className="mt-1 text-[10px] uppercase tracking-wider text-[hsl(var(--muted-foreground))]">reported followers</p></Card>)}</div>}{type === 'peer-benchmarking' && <Card><CardHead title="Reference cohort" meta="synthetic only"/><div className="divide-y divide-[hsl(var(--border))]">{rows.map((row:any)=><div key={row.name} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center"><div className="flex-1"><p className="text-sm font-semibold">{row.name}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{row.note}</p></div><div><p className="font-mono text-sm">{row.posts} <span className="text-[10px] text-[hsl(var(--muted-foreground))]">posts</span></p></div><div className="w-32"><p className="font-mono text-sm">{row.engagement.toLocaleString()} <span className="text-[10px] text-[hsl(var(--muted-foreground))]">eng.</span></p></div></div>)}</div></Card>}{type === 'ai-insights' && <div className="space-y-4">{rows.map((row:Insight)=><Card key={row.id} className="p-6"><div className="flex items-start gap-4"><div className={`mt-1 grid h-8 w-8 shrink-0 place-items-center rounded-lg ${row.type==='signal'?'bg-[hsl(var(--accent)/.5)]':row.type==='caveat'?'bg-[hsl(var(--chart-4)/.15)]':'bg-[hsl(var(--chart-3)/.16)]'}`}><Sparkles size={15}/></div><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-semibold">{row.title}</h3><span className="rounded-full bg-[hsl(var(--muted))] px-2 py-1 font-mono text-[9px] uppercase">{row.confidence} confidence</span></div><p className="mt-3 max-w-2xl text-sm leading-6 text-[hsl(var(--muted-foreground))]">{row.body}</p></div></div></Card>)}</div>}</Page>; }

function ProvenancePage({ type }: { type: 'data-sources' | 'data-import' | 'methodology' }) { const info = { 'data-sources': ['Provenance / Data sources','Know where the evidence came from','A transparent register of the synthetic channels represented in this workspace.'], 'data-import':['Provenance / Data import','Bring a dataset into view','The production boundary is ready for a future FastAPI connector.'], methodology:['Provenance / Methodology','Read the method','Definitions and limits for interpreting this research workspace.'] }[type]; return <Page><SectionIntro eyebrow={info[0]} title={info[1]} description={info[2]}/>{type === 'data-sources' ? <div className="grid gap-4 md:grid-cols-2">{['Facebook public page','Instagram public profile','YouTube channel','X public account'].map((name,i)=><Card className="p-5" key={name}><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[hsl(var(--muted))] text-[hsl(var(--primary))]"><Database size={16}/></span><div><p className="text-sm font-semibold">{name}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Synthetic local source · coverage through Sep 02, 2024</p></div><span className="ml-auto h-2 w-2 rounded-full bg-[hsl(var(--chart-3))]"/></div></Card>)}</div> : type === 'data-import' ? <Card className="p-10"><div className="mx-auto max-w-lg text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-[hsl(var(--accent)/.45)] text-[hsl(var(--primary))]"><Table2 size={20}/></div><h2 className="mt-5 font-serif text-3xl">CSV import boundary</h2><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">This demo keeps the import surface local and intentionally inactive. A future connector can map posts, comments, and account metadata without changing the explorer views.</p><button className="mt-6 rounded-lg border border-[hsl(var(--border))] px-4 py-2.5 text-xs font-semibold hover:bg-[hsl(var(--muted))]" onClick={()=>alert('Demo mode: import is reserved for a future dataset connector.')} data-testid="button-import-csv">Choose CSV file</button></div></Card> : <div className="grid gap-4 lg:grid-cols-2"><Card className="p-6"><h2 className="text-sm font-semibold">How to read this workspace</h2><div className="mt-5 space-y-4 text-sm leading-6 text-[hsl(var(--muted-foreground))]"><p><b className="text-[hsl(var(--foreground))]">Observation window.</b> Jun 01 — Sep 02, 2024. Metrics summarize only the synthetic records within that window.</p><p><b className="text-[hsl(var(--foreground))]">Engagement.</b> Likes + (comments × 2) + (shares × 3). This weighting is a display convention, not a measure of impact.</p><p><b className="text-[hsl(var(--foreground))]">Sentiment and topics.</b> Labels are directional demo classifications intended to make the interface legible.</p></div></Card><Card className="observatory-grid p-6"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-[hsl(var(--primary))]">Important limitation</p><h2 className="mt-4 font-serif text-3xl">Context is not consensus.</h2><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">Social activity is partial, platform-shaped, and not representative of an entire constituency. Treat every view as a research lead requiring corroboration.</p></Card></div>}</Page>; }
function Page({ children }: { children: React.ReactNode }) { return <Shell><div className="observatory-grid min-h-[calc(100dvh-104px)] px-5 py-9 md:px-10 md:py-12"><div className="mx-auto max-w-[1320px]">{children}</div></div></Shell>; }
function NotFound() { return <Page><div className="grid min-h-[60vh] place-items-center text-center"><div><p className="font-mono text-xs uppercase tracking-widest text-[hsl(var(--primary))]">404 / unmapped view</p><h1 className="mt-3 font-serif text-5xl">This signal is quiet.</h1><Link href="/dashboard" className="mt-6 inline-flex rounded-lg bg-[hsl(var(--primary))] px-4 py-2.5 text-xs font-semibold text-[hsl(var(--primary-foreground))]" data-testid="link-return-dashboard">Return to overview</Link></div></div></Page>; }
function App() { return <Switch><Route path="/" component={()=><Redirect/>}/><Route path="/dashboard" component={Overview}/><Route path="/posts" component={Posts}/><Route path="/audience-response" component={Comments}/><Route path="/engagement" component={()=> <AnalyticsPage type="engagement"/>}/><Route path="/topics" component={()=> <AnalyticsPage type="topics"/>}/><Route path="/language" component={()=> <AnalyticsPage type="language"/>}/><Route path="/platforms" component={()=> <AnalyticsPage type="platforms"/>}/><Route path="/peer-benchmarking" component={()=> <AnalyticsPage type="peer-benchmarking"/>}/><Route path="/ai-insights" component={()=> <AnalyticsPage type="ai-insights"/>}/><Route path="/data-sources" component={()=> <ProvenancePage type="data-sources"/>}/><Route path="/data-import" component={()=> <ProvenancePage type="data-import"/>}/><Route path="/methodology" component={()=> <ProvenancePage type="methodology"/>}/><Route component={NotFound}/></Switch>; }
function Redirect() { const [, setLocation] = useLocation(); useEffect(()=>setLocation('/dashboard'), [setLocation]); return null; }
export default App;